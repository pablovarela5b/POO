# include < iostream >
# include  < cstdlib >	
# include  < ctime >
# include < windows.h >
usando el  espacio de nombres  estándar ;

int  main () {
	int y, x;
	srand ( tiempo ( NULL )); // [Instrucción que inicializa el generador de números aleatorios]
	hacer {
		y = rand ();
		x = y% 2 ;
		cout << x;
		Sueño ( 40 );
} mientras ( 1 );
}
